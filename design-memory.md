# Design Memory — Essex Web Crew

Project-local anti-repetition log for crew pitch mockups. `web-design-ultra` Stage 4 reads the **last 3 entries** here (not the global skill log) and bans reusing their font pairing, palette family, or layout archetype on the next prospect — so consecutive prospects look like different studios made them. Stage 8 / the critic appends one row per signed-off mockup.

Columns:
- **Date** — `YYYY-MM-DD`
- **Project** — prospect `<slug>`
- **Font pairing** — heading / body
- **Palette family** — e.g. earthy, dark-moody, warm-editorial, muted-luxury
- **Layout archetype** — e.g. editorial-asymmetric, bento, brutalist-stack, immersive-scroll, service-index
- **Background system** — e.g. gradient-mesh, grain, imagery-driven, contours
- **Signature motion** — entrance family + hero move, e.g. clip-wipe + underline-draw, scale-settle + magnetic (see the skill's `references/motion.md`). ⚠default = the fade-up/text-delay/count-up trio.

| Date | Project | Font pairing | Palette family | Layout archetype | Background system | Signature motion |
|------|---------|--------------|----------------|------------------|-------------------|------------------|
| 2026-07-21 | Cecere Brothers Landscaping | Fraunces / Figtree | forest-editorial | editorial-gallery / service-index | imagery-driven (AI photos) + topographic-contours + grain | fade-up ⚠default + row-expand hover + headline glint |
| 2026-07-21 | anthonys-landscaping — "Evening Estate" | Marcellus / Mulish | dark-evergreen + ivory + brass (dark-luxury) | dark editorial / service-index SPA | flat dark tokens + diagonal texture (built pre-imagery) | not recorded (pre-motion-schema) |
| 2026-07-21 | duran-and-son-landscaping — "Sunday Morning Oasis" | Bricolage Grotesque / Nunito Sans | light daybreak + meadow-green + river-blue + cedar | bright family-friendly stack | light paper tokens (built pre-imagery) | not recorded (pre-motion-schema) |
| ~~2026-07-21~~ | ~~gee-kay-landscaping — "Heritage Ledger"~~ — *superseded by "Property Line" (Rev 2) below; kept for the anti-repetition trail* | ~~Fraunces / Karla~~ | ~~parchment + pine + brass-clay + sage (warm heritage)~~ | ~~ledger / archival editorial~~ | ~~parchment tokens + rule-work (built pre-imagery)~~ | ~~not recorded (pre-motion-schema)~~ |
| 2026-07-22 | john-sessa-cpa — "Balanced Books" | Spectral / Public Sans (+ IBM Plex Mono numerals) | precision-professional (porcelain + ink-navy + manila + verdigris) | sidebar-anchored letterhead | dot-grid graph-paper + teal radial wash + grain | not recorded (pre-motion-schema) |
| 2026-07-22 | cedar-grove-transmission — "Signal & Steel" | Archivo / Barlow | industrial graphite + steel + trans-fluid red | industrial spec-sheet + bento | schematic gear linework + grain + steel glow-orb | not recorded (pre-motion-schema) |
| 2026-07-22 | happy-trees-by-mgm — "Canopy Descent" | Zilla Slab / Work Sans | natural bark + moss + sky + hi-vis lime | canopy-descent full-bleed + rope-stitch scroll | dapple light + photo scrim | not recorded (pre-motion-schema) |
| 2026-07-23 | fora-digital — "Main Street Modern" (our own agency site) | Instrument Serif / Hanken Grotesk | warm-editorial paper + ink + cobalt + clay (BREAK from the dark/tech-slick agency convention; cobalt kept as the trust cue) | gallery-wall portfolio — alternating framed plates + registration-mark hero mat | linen grain + hero radial wash + cool contact wash + **interactive cobalt "compass field" canvas in the hero** | mask-curtain wipe (cobalt panel scaleX, 90 ms `--i` stagger) + ink-sweep hovers (underline-draw / fill-sweep) + gallery-hang `animation-timeline: view()` + **pointer-reactive hero vector field (segments rotate toward the cursor; idle auto-orbit)** — deliberately NOT fade-up, NOT count-up |
| 2026-07-30 | paul-da-silva-law — "Counsel of Record" (**Rev 3**, type+layout overhaul on a client-locked palette) | Libre Caslon Text / Albert Sans (variable) | porcelain-and-ink with brass — the SAME nine tokens as Rev 2, **client-locked and deliberately carried over** (Harry: "keep the colors"); what changed is proportion, light-first at 63% porcelain / 37% ink vs Rev 2's 51% dark. Brass demoted to structure — `--ouro` is text in exactly one CSS rule | modular ruled ledger — Swiss/modular compartments sharing collapsed hairlines, ink bands as punctuation, asymmetric spans (7/5 over 6/6), three rotated section-header treatments | flat porcelain rule-field + two low-alpha corner washes + 0.04 grain + flat `--tinta`→`--tinta-2` ink bands (azulejo lattice and lamp-glow radial RETIRED; no dot grid — reserved to john-sessa) | **rules-draw-in** (scaleX from transform-origin:left, 700ms `cubic-bezier(.16,1,.3,1)`, 60ms `--i` stagger) + **weight-shift** hover on variable Albert Sans (NO lift, NO tilt) + one set-piece: sticky progress index rail, practice-areas only. One ambient: brass glint sweep on the CTA-band phone. GSAP tier 0 — deliberately NOT fade-up/count-up/parallax |
| ~~2026-07-25~~ | ~~paul-da-silva-law — "Ironbound Counsel" (Rev 2)~~ — *superseded by Rev 3 above; kept for the anti-repetition trail* | ~~Besley / Schibsted Grotesk~~ | ~~dark-first iron-gall ink-blue + aged brass + cool porcelain~~ | ~~split-screen advocacy (dark identity panel / light content panel) + document-discipline interior stack~~ | ~~ink grounds + azulejo monoline lattice + grain + warm lamp-glow radial~~ | ~~clip-wipe reveal + card lift/tilt ≤2° + subtle hero parallax~~ |
| 2026-08-02 | gee-kay-landscaping — "Property Line" (**Rev 2**, full rebuild; supersedes the 2026-07-21 "Heritage Ledger" row above, which is struck through) | Newsreader / Familjen Grotesk (both variable; Newsreader roman only — italic serif display is its own detector rule) | plan-paper — cool grey-green ground `#E9EBE4` + graphite ink + spruce `#234A33` as drafting ink + one surveyor's-flag orange `#A83F1E`. Deliberately NOT warm-earth: the four landscapers before this one are all warm-earth or dark-evergreen. `--flag` and `--spruce` never meet as a text pair — that pair was Rev 1's 2.3:1 failure | measured register / site-plan — 7/5 dominant column (never 50/50), hairline datum rules, facts set as measurements at 7rem, 4×2 bento on the services page. Three real HTML pages, not a JS-toggled SPA | tiling surveyor tick-field SVG (96px cross marks, data-URI — not a dot grid, not contours, not a gradient mesh) + feTurbulence grain 0.045 + one spruce radial wash behind the hero column | **slide-alternate** entrance (rows enter from alternating sides, `translateX ∓48px` desktop / `∓20px` mobile, 620ms `cubic-bezier(.2,.75,.25,1)`, 70ms `--i` stagger) — **transform only, no opacity**, so measured hidden text is 0% + **rule-trace edge-lift** hover (a hairline runs the card perimeter as it lifts 4px) + ONE set-piece: **hero-exit** (hero content lifts and fades as the header compacts). Zero ambient systems. GSAP tier 0. Deliberately NOT fade-up, NOT count-up, NOT custom cursor, NOT magnetic, NOT tilt — which was Rev 1's entire motion set |
| 2026-08-03 | dasilva-associates — "The Quiet Verdict" (second, separate build for the same client from Paul's REAL questionnaire; the earlier speculative `paul-da-silva-law` Rev 3 is frozen and NOT superseded — both stand) | Frank Ruhl Libre (900 display) / Red Hat Text — both self-hosted WOFF2, no CDN | **azul + gold, dark-first.** Deep azul grounds `#041E30`/`#062B44`/`#0A3A58`, the firm's own mark blue `#00588A` as the accent on LIGHT only, gold `#C8A24A` as the accent on DARK only, cool paper `#F4F6F7` (deliberately NOT beige). Harry's explicit instruction, replacing the green-and-bone system the plan originally locked. ⚠ **Anti-repetition note for the next build: gold and Rev 3's brass are the same metal-accent family** — the palette axis between these two is PARTIAL, not clean. What separates them is ground and role: Rev 3 is light-first (63% porcelain) with brass demoted to structure; this is dark-first with gold as a full working accent. ⚠ Also: blue-and-gold IS the legal category default Paul called "too vanilla" — distinctiveness here is carried by structure and type, not palette | civic poster-stack — full-bleed photographic hero with the h1 set OVER the image, alternating light/dark full-width bands, oversized real numerals as the hierarchy engine (≥3× scale jump declared as the symmetry break in place of an overlap or dominant column). Four real HTML pages, not a JS-toggled SPA | photographic hero under an azul scrim + triple radial on the dark grounds + feTurbulence grain + three animated god rays anchored to the hero photo's windows. Zero rule-work (deliberately — Rev 3 owns the hairline ruled ledger) | **blur-focus entrance** (opacity + blur(6px) → 0, 600ms `cubic-bezier(.16,1,.3,1)`, 70ms `--i` stagger) + **icon-nudge** hover (the → glyph slides 5px; NO lift, NO tilt, NO scale) + ZERO scroll set-pieces + one ambient (god rays, hero only). GSAP tier 0. Deliberately NOT fade-up, NOT count-up, NOT custom cursor, NOT magnetic. ⚠ Opacity-based, so it measures 23–72% hidden at rest and needs FOUR independent un-hide guards (404 listener, throw catch, dead-man timer, observer-stall backstop); a transform-only entrance like `gee-kay`'s needs none |
<!-- newest at the bottom; append one row per signed-off prospect -->
